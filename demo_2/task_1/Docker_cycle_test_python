#commands should be used in the following order

docker build -t my_image . #image created
docker run --name=first_app my_image #running container called first_app based on my_image image
docker stop first_app
docker rm first_app
docker rmi my_image
