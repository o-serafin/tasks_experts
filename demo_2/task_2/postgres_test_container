docker pull postgres #to download postgres image

docker run --name PostgresTest -p 5455:5432 -e POSTGRES_USER=olafs -e POSTGRES_PASSWORD=postgres123 \
-e POSTGRES_DB=postgresDB -d postgres  #running container with parameters

docker ps #checking if it was succesfull
